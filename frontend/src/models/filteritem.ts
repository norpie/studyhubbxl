export default interface FilterItem{

    /*pub identifier: Uuid,
    pub name: String,
    pub location_type: String,
    pub attributes: Vec<String>,
    pub noise: String,
    pub address: String,
    pub coordinates: Vec<Decimal>,*/

    id: string;
    path: string;
    display_name: string;


}
/*export default interface FilterAttribute{

}

export default interface FilterVolume*/